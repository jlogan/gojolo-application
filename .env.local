# Public config only. All sensitive secrets live in Supabase (see SECRETS.md).
# Copy from Supabase Dashboard → Project Settings → API (API Keys).
# Prefer the new "Publishable key" (sb.publishable.*); legacy anon key also works.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-or-anon-key

# Use your production Supabase project values for local app development.
