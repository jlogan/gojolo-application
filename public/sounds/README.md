# Notification sound

`notification.wav` is generated by `node scripts/ensure-notification-sound.mjs` (also runs before `npm run dev` / `npm run build`).

The app plays the same tone from an in-memory WAV if this file is missing.
